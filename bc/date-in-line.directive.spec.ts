import { DateInLineDirective } from './date-in-line.directive';

describe('DateInLineDirective', () => {
  it('should create an instance', () => {
    const directive = new DateInLineDirective();
    expect(directive).toBeTruthy();
  });
});
